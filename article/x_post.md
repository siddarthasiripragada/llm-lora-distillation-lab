Froze the base model. Trained only LoRA adapters. Tested on held-out SidSearch queries. Base: TBD. LoRA: TBD. The small model did not become smarter at everything. It became better at the one job it was trained and measured on.

