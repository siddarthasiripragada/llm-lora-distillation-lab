Froze the base model. Trained only LoRA adapters.

Held-out SidSearch benchmark:
Base: 0.2542 composite
LoRA: 0.3053 composite

The model did not become smarter at everything. It improved on the one narrow structured task it was trained and measured on.
