I froze almost the entire model and trained only a small set of low-rank matrices. The interesting part was not that training completed. It was whether those adapter weights changed performance on questions the model had never seen.

Results placeholder: do not publish until base and LoRA benchmark files exist.

