# Architecture

SidSearch examples are generated from the protocol, validated, split deterministically, and used for supervised LoRA fine-tuning. The base model remains frozen while adapter matrices receive gradient updates.

