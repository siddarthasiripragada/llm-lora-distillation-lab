# Evaluation Methodology

All benchmark systems must use the frozen held-out set. Metrics include exact field accuracy, JSON validity, schema compliance, entity F1, applied-rule F1, filter accuracy, latency, category breakdowns, and confidence intervals.

